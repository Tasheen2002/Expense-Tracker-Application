{
  "name": "@apps/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon --exec ts-node src/outbox.run.ts",
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "bullmq": "^5.1.0",
    "@packages/core": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}
