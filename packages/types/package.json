{
  "name": "@expense-tracker/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types used across apps and packages (DTOs, enums, IDs, API envelope).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@expense-tracker/typescript-config": "workspace:*",
    "typescript": "^5.4.0"
  }
}
